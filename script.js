(()=>{"use strict";const body=document.body,header=document.querySelector('[data-header]'),toggle=document.querySelector('.menu-btn'),nav=document.querySelector('.nav'),prog=document.querySelector('.progress span'),reduced=matchMedia('(prefers-reduced-motion: reduce)').matches;if(toggle&&nav){const close=()=>{toggle.setAttribute('aria-expanded','false');nav.classList.remove('open');body.style.overflow=''};toggle.addEventListener('click',()=>{const open=toggle.getAttribute('aria-expanded')==='true';toggle.setAttribute('aria-expanded',String(!open));nav.classList.toggle('open',!open);body.style.overflow=!open?'hidden':''});nav.querySelectorAll('a').forEach(a=>a.addEventListener('click',close));document.addEventListener('keydown',e=>{if(e.key==='Escape')close()})}const onScroll=()=>{const max=document.documentElement.scrollHeight-innerHeight,p=max>0?scrollY/max*100:0;prog&&(prog.style.width=`${Math.max(0,Math.min(100,p))}%`);header&&header.classList.toggle('scrolled',scrollY>24)};onScroll();addEventListener('scroll',onScroll,{passive:true});const hours={0:[12,22],1:[17,23],2:[17,23],3:[17,23],4:[17,23],5:[16,23],6:[12,23]};function manchester(){const parts=new Intl.DateTimeFormat('en-GB',{timeZone:'Europe/London',weekday:'short',hour:'2-digit',minute:'2-digit',hour12:false}).formatToParts(new Date()),m=Object.fromEntries(parts.map(p=>[p.type,p.value])),d={Sun:0,Mon:1,Tue:2,Wed:3,Thu:4,Fri:5,Sat:6}[m.weekday];return{day:d,h:+m.hour,min:+m.minute,time:`${m.hour}:${m.minute}`}}function tick(){const n=manchester(),[o,c]=hours[n.day],cur=n.h*60+n.min,open=cur>=o*60&&cur<c*60;document.querySelectorAll('[data-clock]').forEach(e=>e.textContent=n.time);document.querySelectorAll('[data-status]').forEach(e=>e.textContent=open?`OPEN · UNTIL ${String(c).padStart(2,'0')}:00`:'CLOSED NOW');document.querySelectorAll('.hours-list>div').forEach(r=>r.classList.toggle('today',+r.dataset.day===n.day))}tick();setInterval(tick,30000);const reveals=[...document.querySelectorAll('.reveal')];if(reduced||!('IntersectionObserver'in window))reveals.forEach(e=>e.classList.add('in'));else{const io=new IntersectionObserver(es=>es.forEach(x=>{if(x.isIntersecting){x.target.classList.add('in');io.unobserve(x.target)}}),{threshold:.13});reveals.forEach(e=>io.observe(e))}if(!reduced&&matchMedia('(hover:hover) and (pointer:fine)').matches){document.querySelectorAll('[data-tilt]').forEach(el=>{el.addEventListener('pointermove',e=>{const r=el.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;el.style.transform=`perspective(900px) rotateX(${y*-3}deg) rotateY(${x*4}deg)`});el.addEventListener('pointerleave',()=>el.style.transform='')});const dot=document.querySelector('.cursor-dot'),ring=document.querySelector('.cursor-ring');let rx=0,ry=0,tx=0,ty=0;document.addEventListener('pointermove',e=>{tx=e.clientX;ty=e.clientY;dot.style.left=tx+'px';dot.style.top=ty+'px'});(function loop(){rx+=(tx-rx)*.16;ry+=(ty-ry)*.16;ring.style.left=rx+'px';ring.style.top=ry+'px';requestAnimationFrame(loop)})();document.querySelectorAll('a,button,[data-tilt]').forEach(el=>{el.addEventListener('pointerenter',()=>ring.classList.add('hover'));el.addEventListener('pointerleave',()=>ring.classList.remove('hover'))})}})();